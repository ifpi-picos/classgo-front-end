export default function Body({children}) {
    return (
        <div className="flex justify-center items-center w-full h-screen bg-white text-neutral-800">
            {children}
        </div>
    )
}
